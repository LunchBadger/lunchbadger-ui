// import ForecastService from '../services/ForecastService';

export default [
  state => {
    // const {entities: {forecasts}} = state; //TODO - when Forecasts API starts to work on backend side
    // Object.keys(forecasts).forEach((key) => {
    //   ForecastService.save(forecasts[key].toJSON());
    // });
    return {};
  },
];
